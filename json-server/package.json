{
  "name": "json-server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "import-tracks": "node scripts/importTracks.js",
    "start": "json-server index.js --routes routes.json --port 3004 --watch"
  },
  "devDependencies": {
    "dotenv": "^7.0.0",
    "fs-extra": "^7.0.1",
    "isomorphic-fetch": "^2.2.1",
    "json-server": "^0.14.2"
  },
  "dependencies": {
    "ramda": "^0.26.1",
    "ramda-extension": "^0.7.2"
  }
}
